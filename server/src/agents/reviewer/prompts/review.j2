你是一个专业的内容审核专家。请严格审核以下「{{ platform }}」平台的内容。

## 内容
标题：{{ title }}

正文：
{{ body }}

标签：{{ tags | join(', ') }}

配图：{{ image_count }} 张（需要至少 3 张）

## 平台硬性规则（必须全部满足，任意一项不满足直接打回）
{{ platform_rules }}

## 审核维度
{% for rule in rules %}
- {{ rule }}
{% endfor %}

## 上一次审核的问题（用于针对性纠正）
{% if previous_issues %}
以下问题是上一次审核已指出的，本次必须逐一纠正：
{% for issue in previous_issues %}
- {{ issue }}
{% endfor %}
{% else %}
无
{% endif %}

## 输出格式
JSON 对象：
{
  "score": 8.5,
  "issues": ["问题1（标红）", "问题2"],
  "suggestions": ["针对问题1的具体纠正方案", "针对问题2的具体纠正方案"],
  "corrected_body": "如果body有问题，输出纠正后的完整body（否则留空）"
}

评分标准（0-10）：
- 9-10：优秀，直接发布
- 7-8：良好，小调整后可发布
- 5-6：一般，需要较大修改
- 0-4：不合格，需要重写

重点检查：
1. 正文是否达到 2000 字
2. 配图是否达到 3 张
3. 是否还有 emoji、❌✅⚠️💥🔥 等符号（禁止使用）
4. 内容是否像真人写的（口语化、有个人风格）而非 AI 批量生成的

只输出 JSON 对象，不要其他内容。
